{
  "name": "ast-grep-matcher-performance",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && npx tsc && node dist/main.js"
  },
  "type": "module",
  "dependencies": {
    "@ast-grep/napi": "^0.36.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.7.1"
}
